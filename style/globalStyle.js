import { StyleSheet } from "react-native";
 const global = StyleSheet.create({
    textStyle:{
        fontSize:16,
        fontWeight:'bold'
    },
    redText:{
        fontSize:16,
        fontWeight:'bold',
        color:'red',
    }
    
});
export default global;